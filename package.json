{
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "postinstall": "husky install",
    "generate:gitignore": "node scripts/generate-gitignore.js",
    "husky:setup": "husky install && chmod +x .husky/pre-commit"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
